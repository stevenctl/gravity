package com.sugarware.gravity;

public class Angles {
	public static final float UP = (float) (Math.PI / 2),
							  DOWN = (float) (3 * Math.PI / 2),
							  RIGHT = 0,
							  LEFT = (float) Math.PI;
}
